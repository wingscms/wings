{
  "name": "@wingsplatform/gatsby-plugin",
  "version": "0.7.2",
  "description": "A Gatsby plugin for Wings",
  "main": "index.js",
  "author": "Maarten Busstra <busstra.m@gmail.com>",
  "keywords": [
    "gatsby",
    "gatsby-plugin",
    "wings"
  ],
  "license": "MIT",
  "scripts": {
    "start": "npm run build -- --watch",
    "build": "babel src --out-dir .",
    "prepublish": "cross-env NODE_ENV=production npm run build"
  },
  "dependencies": {
    "babel-runtime": "^6.26.0",
    "node-fetch": "^1.7.3"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "cross-env": "^5.0.5"
  },
  "peerDependencies": {
    "@wingsplatform/content": "^0.5.1"
  }
}
