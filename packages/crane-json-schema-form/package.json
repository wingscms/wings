{
  "name": "@wingscms/crane-json-schema-form",
  "version": "0.0.0",
  "description": "JSON schema forms for Wings' Crane component library",
  "main": "build/index.js",
  "scripts": {
    "start": "npm run build -- --watch",
    "build": "babel src --out-dir build --copy-files",
    "prepublish": "cross-env NODE_ENV=production npm run build"
  },
  "author": "Mark Jackson <markjackson@bureaubolster.nl>",
  "devDependencies": {
    "babel-cli": "6.26.0",
    "cross-env": "6.0.3"
  },
  "dependencies": {
    "react-jsonschema-form": "1.8.1",
    "react": "^16.12.0",
    "react-dom": "^16.12.0"
  },
  "peerDependencies": {
    "styled-components": "^3 || ^4"
  }
}
